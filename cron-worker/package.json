{
  "name": "delta-keepalive-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker Cron Trigger to ping DELTA Render Go web service every 13 minutes",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test-cron": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.2"
  }
}
